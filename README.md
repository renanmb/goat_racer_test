# Goat Racer Collab

This repo contains the:

- Goat Racer One extension under ```goatracer.one.interactive```.
- Scripts for configuring Brev instance ```brev-launchable-scripts```.
- IsaacLab project to train the car.


## Deliverables

1. ✅ Consolidation of IsaacSim extensions already created into a single GitHub repository:

- ✅ Fix installation instructions to install a single extension and have a simple UI based install.
- ✅ All python dependencies should be automatically installed if not present in isaacsim python environment.

2. Brev launchable or cloud setup script to provision a machine that will install everything needed to run the GOAT RACER ONE training and viewing of trained policy.

- ✅ IsaacSim 5.1, IsaacLab 2.3.x.
- ❌ All extensions and training code present in container or image generated.
- ✅ Ideally a turn-key script that can be used for a single user for now to prepare a machine to perform all labs associated with GOAT RACER ONE as if they were on a local machine.
- ✅ Visual display of machine’s desktop through VNC or website interface.
- Documentation on how to revise the version of each dependency:
    - ❌ Isaacsim
    - ❌ Isaaclab
    - ❌ GOAT RACER ONE training code
    - ✅ GOAT RACER ONE extension
- ❌ Documentation on how to deploy for a single user on Brev platform.    
- All placed in above github repository ```goat_racer_collab```.

